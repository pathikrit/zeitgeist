[project]
name = "zeitgeist"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "marimo>=0.14.11",
    "pydantic-ai>=0.4.3",
    "requests>=2.32.4",
]
[tool.marimo.runtime]
dotenv = [".env"]